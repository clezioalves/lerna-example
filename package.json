{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.10.7",
    "lerna": "^8.1.9",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}